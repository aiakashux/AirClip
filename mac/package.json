{
  "name": "clipr-mac",
  "version": "0.1.0",
  "description": "Clipr+ Mac client — E2E encrypted clipboard sync",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && cp src/renderer/*.html dist/renderer/",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "libsodium-wrappers": "^0.7.15",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/libsodium-wrappers": "^0.7.14",
    "@types/ws": "^8.5.13",
    "electron": "^33.3.1",
    "typescript": "^5.7.3"
  }
}
